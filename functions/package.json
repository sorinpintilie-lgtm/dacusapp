{
  "name": "dacus-firebase-functions",
  "private": true,
  "type": "module",
  "engines": {
    "node": "22"
  },
  "main": "index.mjs",
  "scripts": {
    "build:api": "npm run build -w @dacus/api",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions:api"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.2",
    "dotenv": "^16.4.7",
    "firebase-admin": "^13.0.2",
    "firebase-functions": "^7.1.1",
    "fastify": "^5.2.1",
    "typesense": "^2.0.0",
    "zod": "^3.24.2"
  }
}
